<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>New post "<%= @post.title %>" in topic "<%= @topic.title %>"</h1>
    <small>
      By: <%= @post.user.name %>
    </small>
    <p>
      <%= @post.body %>
    </p>
    <p>
      <%= link_to "View Comment on site", topic_post_url(@post.topic, anchor: "post-#{@post.id}") %>
    </p>
  </body>
</html>
