<h1>All Advertisements</h1>
<% @advertisements.each do |a| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <p><%= link_to a.title, a %> $<%= a.price %></p>
      </h4>
    </div>
  </div>
<% end %>
